import { PreventLinebreakDirective } from './prevent-linebreak.directive';

describe('PreventLinebreakDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventLinebreakDirective();
    expect(directive).toBeTruthy();
  });
});
